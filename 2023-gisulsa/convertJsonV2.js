// var fs = require('fs');
// var jsonObj = fs.readFileSync('./public/images.json', 'utf-8');
// jsonObj = JSON.parse(jsonObj);
// var keys = Object.keys(jsonObj);
// keys.forEach(key => {
//   jsonObj[key] = {
//     date: 'last',
//     list: jsonObj[key]
//   }
// });

// fs.writeFileSync('./public/imagesV2.json', JSON.stringify(jsonObj, null, 2), 'utf-8');
// 그냥 새로 크롤링 함.